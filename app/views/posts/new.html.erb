<%= simple_form_for @post do |f| %>
    <%= f.input :board_id, collection: Board.all, prompt: '게시판을 선택하세요' %>
    <%= f.input :title %>
    <%= f.cktext_area :content, cols: 40, ckeditor: { uiColor: '#f2f2f2', toolbar: 'mini' } %>
    <p></p>
    <%= f.button :submit %>
<% end %> 